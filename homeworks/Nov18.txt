@DarkSavant  телеграмм  преподавателя Бахтинова АНдрея

IT - программа (система контроля версий, вспом. инструмент любого разработчика)

GITHUB - хостинг для git (репо)

Репозиторий - файловая БД проекта с историей разработки (.git/)
	(хранилище)
ls - листинг каталогов/файлов

pwd (print working dir) - показать тек/каталог

cd (change dir) - смена каталога

(без аргументов - вернуться в дом/каталог)

mkdir (make dir) - создать каталог

rm (remove) - удалить файл/каталог

mv (move) - переместить/переименовать

cp (copy) - копировать

cat - вывод содержимого файла в терминал

id - вывод информации о пользователе

uname - вывод информации о системе
==
cd (перейти в дом/каталог)
mkdir backups (создать каталог)

~ (дом/каталог)
. (тек/каталог)
.. (каталог выше)

root - название пользователя по умолчанию
(главного администратора компьютера)

vi  выход :q!

ЛИЕНТ-СЕРВЕРНАЯ АРХИТЕКТУРА (СХЕМА "ЗАПРОС-ОТВЕТ")

	- Это модель взаимодействия между сетевыми узлами
	

	(1) Клиент (роль) - активная сторона, выполняет запросы
	(2) Сервер (роль) - пассивная сторона, принимает запросы и возвращает ответ
	
https://bellard.org/jslinux/vm.html?url=alpine-x86.cfg&mem=192

mkdir -p dir1/dir2/dir3/ - создает сразу все каталоги

rm -rf dir

	удалить каталог
	-r (рекурсивно)  под каталоги
	-f (для каталогов)

df -h - disk free, вывести информацию о дисках, -h  человеко читаемеы цифры размеров
du -hs (показывает количество фалйов и размер)
htop -  процессы
uptime
